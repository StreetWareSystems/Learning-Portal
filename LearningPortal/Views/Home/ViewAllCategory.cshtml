
@{
    ViewBag.Title = "ViewAllCategory";
    Layout = "~/Views/Shared/_Layout.cshtml";
    <title>Categories</title>
}


@model IEnumerable<LearningPortal.Models.Categories>




<div class="mdk-drawer-layout__content page mt-5">
    <div class="media align-items-center mb-headings">
        <div class="media-body">
            <h1 class="h2">Categories</h1>
        </div>
    </div>
    <div class="clearfix"></div>
    <div class="card-columns row">
        @{ foreach (var item in Model)
            {
                string que = helpper.Encrypt("" + item.CategoryId, true);
                que = que.Replace('%', 'a');
                que = que.Replace('+', '!');
                <div class="carousel-item  col-sm-6 col-md-4 col-lg-3  active">
                    <a href="@Url.Action("ViewAllSubCategory", "Home", new { id =  que.Replace('/', '$')})">
                        <div class="card card-sm">

                            <div class="position-relative">
                                <div class="card-img-top">
                                    <img src="~/assets/images/categories/@item.Image"
                                         class="card-img-top">
                                </div>

                            </div>
                            <div class="card-body d-flex">
                                <div class="flex">


                                    <h5 class="card-title m-0" style="text-align:center">@item.CategoryName</h5>


                                </div>

                            </div>

                        </div>
                    </a>
                </div>
            }
        }
    </div>
</div>